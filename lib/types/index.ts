/**
 * Shared types re-exported from feature modules.
 * Use this for cross-feature imports instead of importing directly between features.
 */
export type { PlaceSummary, Place, PlacesResponse, PlaceDetailsResponse } from '@/features/places/types';
export type { Friend } from '@/features/social/types';
