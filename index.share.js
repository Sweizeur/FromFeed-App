/**
 * Point d'entrée de la Share Extension (expo-share-extension exige index.share.js).
 * Toute la logique et les styles sont dans index.share.tsx (TypeScript + theme).
 */
import './index.share.tsx';
